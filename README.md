This repository contains two branches:
backend: A Laravel API that includes authentication (via Sanctum), CRUD operations, and API resources.
frontend: An Angular application that interacts with the Laravel API to display data.

Project Structure
backend (Laravel): Contains the API, authentication system, and database seeding for the login system.
frontend (Angular): Contains the Angular application to interact with the backend and display data.
